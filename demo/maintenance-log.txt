Saturday maintenance window notes. Started 21:00, ended 23:40. Rotated the TLS certificates on the reverse proxy and confirmed auto-renewal is scheduled for sixty days out. Patched the hypervisor to the current stable kernel and rebooted both app nodes one at a time, no dropped sessions. Cleaned up 41 GB of stale container images from the registry host and set a weekly prune job so it stops accumulating. The monitoring agent on the storage box was pinning a core every few hours; upgraded it and the flame graph looks sane again. While the proxy was drained we also relocated the wiki service off the shared node and it now listens on port 8090 behind the proxy, old bookmarks redirect. Database vacuum ran clean, index bloat is back under ten percent. The nightly backup job had been overlapping with the log shipper at 02:30, staggered them by twenty minutes and both finish comfortably now. Firmware on the switch is one release behind but the changelog is all wifi fixes we do not use, deferring. Left the spare NVMe in the tray as a cold standby. Next window: move the metrics retention from 30 to 90 days once the new disk is in, and test restore from Saturday's backup set on the scratch VM.
